# 伴柠番茄钟 · 专注页内测埋点方案

> 版本：v1.0 ｜ 更新：2026-08-30 ｜ 状态：待实施
> 目标：为提升用户与 AI 的互动体验和学习沉浸度，通过埋点获取可归因决策数据。

---

## 一、背景与目标

专注页进入内测。当前判断 AI 互动与沉浸度完全靠感觉，需要埋点把「玄学感知」变成「可归因决策」。

### 要回答的 5 个问题

| 优先级 | 问题 | 直接决定什么改动 |
|---|---|---|
| P0 | 沉浸度：完成率多少？第几分钟放弃最多？暂停多频繁？ | 番茄钟时长、AI 开口频率该不该调 |
| P0 | AI 链路健康：开场白/视觉/语音/TTS 每一步成功率、延迟，哪一环掉链子 | 先修 bug 还是先加功能 |
| P1 | AI 互动深度：一个番茄钟里 AI 开口几次、用户回了几句、开了摄像头/语音没有 | 判断「陪伴感」是真的还是自嗨 |
| P1 | 学习行为：任务/状态/时长选择分布、杂念拦截了几个 | 任务流和状态选择器的体验 |
| P2 | 质量：报错率、切后台/退出点 | 稳定性 |

### 北极星指标

**番茄钟完成率** × **每番茄钟主动 AI 交互次数**（发消息 + 语音 + 点 OC）。

### 一个更深的专项：用户希望 AI 主动说话的频率与内容

「用户希望 AI 多主动说话」不能问，要靠测：AI 每次主动说话是一次**打断**，用户只有三种选择——忍受、反击、逃离。埋点测的是行为反应：

| 用户反应 | 行为信号（可检测） | 含义 |
|---|---|---|
| 接受 / 无感 | 继续专注（下轮 vision 仍 STUDYING）、无操作 | 这口说多了也能忍 |
| 被打断 | 语音播放中暂停/退出/切后台/关摄像头 | 强打扰 → 频率太高 |
| 接话 | 发消息、语音回话、点 OC | 互动意愿高 → 内容/时机对了 |
| 逃离 | 说话后 15s 内 stopTimer / 退出页面 | 最严重负反馈 |

核心产出：**「开口频率-用户忍受度」曲线**，找到策略参数（`AMBIENT_POLICY` / `SPEECH_POLICY`）该停在的区间。

---

## 二、设计原则（对齐现有架构）

1. **Local-First + 批量上报**——沿用 `saveLog()` / `SyncManager` 模式：事件先写 localStorage 队列，离线攒着，在线批量上报。
2. **会话贯穿**——复用 `focusCompanion.state.epoch` 做 `session_id`，所有事件带它 + 事件发生时的已专注秒数，可完整还原时间线。
3. **只埋元数据，不埋内容**——内测阶段不上报聊天原文和摄像头画面，只报类型/长度/耗时/结果。隐私友好、成本不涨。
4. **分层**——`companion_logs` 继续当 AI 管线**调试日志**（高容量、24h 保留、含图）；新建 `events` 表当**产品行为分析**（追加写、长期保留、不含图），两套分离。
5. **两套 UI 都覆盖**——经典页（`index.html` + main.js/chat.js）与 Beta 陪伴页（`prototype-ui.js`，`?mode=beta` / `/beta`），事件带 `ui` 维度区分。

---

## 三、事件模型

统一事件格式：

```json
{
  "event": "focus_complete",
  "ts": 1693483200000,
  "session_id": "1693483200000",
  "user_id": "uuid 或 null",
  "device_id": "tamotoPushDeviceId",
  "ui": "classic | beta",
  "mode": "pomodoro | timer",
  "elapsed": 1500,
  "policy_version": "v1",
  "props": {}
}
```

### 通用字段约定

| 字段 | 来源 |
|---|---|
| `session_id` | 复用 `focusCompanion.state.epoch`（一次专注 = 一个会话） |
| `device_id` | **复用** Push 体系的 `tamotoPushDeviceId`（pwa.js 生成的 randomUUID），不另造一套 |
| `user_id` | `syncManager.userId`，未登录为 null；同手机号可跨设备关联 |
| `ui` | `'classic'` 经典页 / `'beta'` Beta 陪伴页 |
| `policy_version` | AI 开口策略版本，改 `AMBIENT_POLICY` 时 bump，否则无法对比新旧策略数据 |

---

## 四、事件清单

### A. 会话生命周期 —— 沉浸度核心

| 事件 | 触发点 | 关键属性 | 回答的问题 |
|---|---|---|---|
| `focus_session_start` | `startStopTimer` 成功进入 | mode、selectedMinutes、taskName、statusName、ocName | 多少人点了开始？被「状态未选」卡住的比例 |
| `timer_pause` / `timer_resume` | `pauseResumeTimer` | elapsed | 中断频率 → 沉浸度 |
| `focus_abandon` | `stopTimer` 中途停止 | elapsed、remaining | 放弃点在第几分钟 |
| `focus_complete` | `finishRunningTimer` | duration、taskName、statusName | 完成率 |
| `focus_page_exit` | pwa.js `registerFocusAway`/`registerFocusReturn` | elapsed、awaySeconds | 切后台时长 → 最硬沉浸度信号 |
| `focus_session_end` | `focusCompanion.stopSession` | 全量会话汇总（见下） | 沉浸度指数 |

**`focus_session_end` 压轴事件**——`stopSession` 时把会话所有统计一次性汇总：

```
{ completed, abandoned, plannedMinutes, totalElapsed, pauseCount,
  aiSpeechCount, aiSpeechCompleted, aiSpeechFailed,
  userMessageCount, voiceInputCount, cameraOnSeconds,
  distractionCount, giftReceived }
```

### B. AI 互动 —— 互动体验核心

| 事件 | 触发点 | 关键属性 |
|---|---|---|
| `ai_speech` | **统一收口**所有 AI 开口（visual / opening / opening_event / presence / praise / activity / encourage / dialogue / session_opening） | source、type、speechTopic、textLen、ttsStatus(completed/suppressed/failed)、ttsBytes、latencyMs、speechId |
| `ai_speech_reaction` | 每条 AI 语音播放完，开 15s 反应窗口 | speechId、source、type、reaction(focus_continued/engaged/interrupted/rejected)、reactionMs |
| `ai_speech_interrupt` | AI 语音播放**中途**被打断（`cancelReaction` 各处） | speechId、source、by(pause/stop/voice/pagehide) |
| `ai_vision` | `analyzeCurrentFrame` 成功 | state、shouldSpeak、visionMs/reactionMs/totalMs |
| `ai_opening` | `prepareOpening`/`playPreparedOpening` | prepared(ok/fail)、played(ok/fail)、latencyMs |
| `ai_phrase` | 语录池：`startEncourageLoop` 轮播 / `showRestMessage` / `showRemindMessage` / `handleOCClick` | phraseType(encourage/remind/annoyed/angry/rest)、from(主动轮播/用户点OC) |
| `user_chat_send` | `sendChatMessageToOC` | msgLen、isDistraction、isReminder、replyOk、replyLen、latencyMs |
| `user_voice_input` | `startVoiceInput` 结束 | result(success/no_speech/error)、durationMs |
| `camera_toggle` | `toggleCamera` | enabled、permission(allow/deny) |
| `oc_click` | `handleOCClick` | — |

> `speechId = epoch-turnId-source`，`ai_speech` 与 `ai_speech_reaction` 通过它关联，才能做「某种内容 → 某种反应」分析。

### C. 主动说话机制决策层 —— 调 `AMBIENT_POLICY` 的依据

| 事件 | 触发点 | 关键属性 |
|---|---|---|
| `speech_policy_decision` | 每次视觉 `analyzeCurrentFrame` 拿到 `result.decision`（**无论说不说都上报**） | state、shouldSpeak、reason（pipeline 中文原因直接透传）、sinceLastSpeechMs、policyKey |
| `ambient_decision` | `considerAmbientSpeech` 的**每个 return 分支** | decided(skip_early/skip_unsafe/skip_event_cooldown/skip_ambient_cooldown/skip_limit/skip_roll/speak)、type、studyPhase、speakChance、ambientCount、sinceLastAmbientMs |

- `speech_policy_decision` 回答：**AI 想说话但被策略压住了多少次、为什么压** → 冷却值卡得松还是紧。
- `ambient_decision` 回答：`firstTenMinutesLimit: 20` 等常量**实际在拦掉多少话** → 调参有据。
- `policyKey` 由 reason 归类：`phone_threshold` / `first_encourage` / `return` / `absent` / `rest` / `light` / `silent`。

### D. 学习行为

| 事件 | 触发点 | 关键属性 |
|---|---|---|
| `task_select` | 任务选择确认 | taskName |
| `status_select` | `selectStatus` | statusName |
| `time_select` | `selectTime` / 自定义 | minutes |
| `mode_switch` | 番茄钟/正计时切换 | mode |
| `distraction_add` | 杂念记录 | — |
| `completion_view` | `showCompletionModal` | giftType |

### E. 体验质量

| 事件 | 触发点 |
|---|---|
| `api_error` | 统一封装：chat / observe / tts / speech / reminders 出错 |
| `vision_error` | `analyzeCurrentFrame` catch |
| `tts_error` | `playStreamingTts` 失败 |
| `network_offline` | online / offline 事件 |

### F. Beta 专属

| 事件 | 触发点 | 说明 |
|---|---|---|
| `beta_meeting_view` / `beta_meeting_submit` | `showBetaMeeting` / `submitBetaMeetingTask` | 见面流程：任务输入 → 开场回应 |
| `beta_body_double` | `rollBodyDouble` / `renderBodyDoublePlan` | 身体同步计划使用率 |
| `bgm_toggle` | `bnRainBtn` / `cycleBgm` | 背景音乐开关 |
| `focus_end_confirm` | `bnPause` 800ms 长按 → `showEndConfirm` | 带 `lastSpeechAgoMs`，判断是否被 AI 烦走 |
| `voice_hold` | `bnVoiceInputBtn` pointerdown/up | 按住说话时长分布 |

### G. 实验层

`policy_version` 字段随所有 AI 决策/开口事件上报。改 `AMBIENT_POLICY` 时 bump 版本号，无版本号则新旧策略数据无法对比。

---

## 五、分析看板

复用 `companion-logs.html` 模式新建 `events.html`，内测期盯以下视图：

### 1. 转化漏斗
`进专注页 → 选任务/状态 → 开始 → 完成` 每步人数与转化率。
卡在哪步就改哪步（开始按钮门槛、时长、入口清晰度）。

### 2. 放弃分布直方图
放弃事件按 `elapsed` 分桶。
- 集中前 3 分钟 = 开局体验问题（开场白没接住）
- 集中 20-25 分钟 = 时长太长，拆番茄

### 3. AI 互动卡
- **开口密度 vs 完成率散点**：每番茄钟开口次数 ↔ 完成率，验证「AI 说得多 → 更易坚持」还是「反而烦」
- **互动渗透率**：发消息/语音/开摄像头/点OC 的使用率
- **TTS 成功率**：AI 说话用户实际听到的比例

### 4. 互动质量时间线（单会话还原）
选中一个 `session_id` 看完整时间线，检查 AI 有没有「接住」用户关键时刻（暂停后、发牢骚后、开摄像头后）。

### 5. 链路健康
vision p50/p95 耗时、vision 错误率、TTS 失败率与失败阶段。

### 6. 开口频率容忍度曲线 ★核心
```
x = 每番茄钟 AI 主动开口次数
y1 = 完成率    y2 = 负反馈率(interrupted + rejected)
```
找拐点：开口次数超过 N 后负反馈陡升 → **N 就是话痨天花板**。

### 7. 内容偏好矩阵 ★核心
```
           engaged率    interrupted率    样本数
encourage    48%          3%            214
praise       61%          1%            96
activity     22%          11%           88
presence     18%          9%            130
```
praise 受欢迎就加权重；presence/activity 负反馈高就减或改文案。

### 8. 时机分布
- 开口发生在第几分钟 vs 反应（前 5 分钟接受度高，20 分钟后人烦躁？）
- 距上次开口间隔 vs 反应（<2min 负反馈高 → 冷却值该加）

### 9. 学习行为
时长选择分布、任务切换频率、状态选择分布、杂念拦截数。

---

## 六、技术实现

### 6.1 新增 `supabase/events.sql`

```sql
-- 产品行为分析表（追加写、长期保留、不含图片/对话内容）
CREATE TABLE IF NOT EXISTS events (
  id BIGSERIAL PRIMARY KEY,
  event TEXT NOT NULL,
  ts TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  session_id TEXT,
  user_id UUID,
  device_id TEXT,
  ui TEXT,
  mode TEXT,
  elapsed INT,
  policy_version TEXT,
  props JSONB NOT NULL DEFAULT '{}'
);
CREATE INDEX IF NOT EXISTS idx_events_name_ts ON events(event, ts DESC);
CREATE INDEX IF NOT EXISTS idx_events_session ON events(session_id);
CREATE INDEX IF NOT EXISTS idx_events_device ON events(device_id);
```

> ⚠️ 注意：`companion_logs` 目前没有任何建表脚本（在 Supabase Dashboard 手工建的）。新表脚本务必入库，避免失联。

### 6.2 新增 `api/track.js`

Vercel Serverless 批量上报端点：接收事件数组 → 一次批量 insert 进 `events` 表（append-only，不回写）。参考 `api/companion-logs.js` 的 `supabaseFetch` 模式。

### 6.3 新增 `frontend/js/track.js`（埋点 SDK）

```js
window.track(eventName, props)   // push 进 localStorage 队列 'tamoto_events_v1'
track.sessionStart(epoch, meta)  // 设置会话上下文
track.sessionEnd()               // 触发 focus_session_end 汇总
track.startReactionWindow(speechId, meta)  // AI 语音播放后开 15s 反应窗口
```

- 攒 10 条 或 30 秒 批量上报（`navigator.onLine` 才发，成功清队列，失败保留重试）
- 队列上限 500 条，防止 localStorage 无限膨胀
- 自动注入 `session_id` / `user_id` / `device_id` / `ui` / `mode` / `elapsed` / `policy_version`
- `device_id` 读取 `tamotoPushDeviceId`，缺失则生成

### 6.4 埋点挂点清单

| 文件 | 挂点 |
|---|---|
| `frontend/js/main.js` | `startStopTimer`(3710)、`pauseResumeTimer`(3800)、`stopTimer`(3865)、`finishRunningTimer`(992)、`showPomodoroComplete`(4087)、`selectStatus`、`selectTime`、`showTaskSelector`、`handleOCClick`(1334)、`generateFocusPhrases`、`startEncourageLoop`(1281)、`showRestMessage`(1303)、`showRemindMessage`(1309) |
| `frontend/js/focus-companion.js` | `analyzeCurrentFrame`(499) 成败、`considerAmbientSpeech`(861) 每个 return 分支、`playMessageSequence`(457) 完成处开反应窗口、`cancelReaction`(340) 埋 interrupt、`startVoiceInput`(894)、`toggleCamera`(181)、`prepareOpening`(656)/`playPreparedOpening`(692)、`startSession`(190)/`stopSession`(219) |
| `frontend/js/chat.js` | `sendChatMessageToOC`(648) |
| `frontend/js/pwa.js` | `registerFocusAway`(88)/`registerFocusReturn`(105) |
| `frontend/js/prototype-ui.js` | `showBetaMeeting`(443)、`submitBetaMeetingTask`(475)、`rollBodyDouble`(547)、`cycleBgm`(631)、`showEndConfirm`(带 lastSpeechAgoMs) |
| `lib/companion-pipeline.js` | `evaluateSpeechPolicy`(95) 返回里加 `speechTopic` 标签；`runCompanionPipeline`(366) 透传 policyKey |
| `index.html` | 引入 `<script src="frontend/js/track.js">`（在 main.js 之前）；页面隐藏时 flush |

### 6.5 AI 开口策略可实验化（配合 `policy_version`）

`AMBIENT_POLICY`（focus-companion.js:48）目前是硬编码常量。改动方案：

1. 常量改为可从 localStorage 注入覆盖（键 `bn_policy_overrides`，内测改策略时写入）
2. 同时记录生效的 `policy_version`（localStorage `bn_policy_v`，bump 即切换）
3. 改策略 = 改 localStorage 一行，数据自动打上新版本号

---

## 七、实施步骤

1. 建 `supabase/events.sql`（执行建表）+ 新增 `api/track.js` 上报端点
2. 写 `frontend/js/track.js` SDK，引入 index.html
3. main.js / chat.js 埋「会话生命周期 + 学习行为」（A、D 类）
4. focus-companion.js 埋「AI 互动 + 决策层」（B、C 类），含反应窗口跟踪器
5. prototype-ui.js 埋 Beta 专属（F 类）
6. 建 `events.html` 内测看板
7. 内测首日检查：上报量、事件命名一致性、localStorage 队列无泄漏、无隐私内容入库

---

## 八、隐私与合规注意

- 内测仅采集行为元数据，**不上报聊天原文、摄像头画面、识别文本**
- 埋点上线前向内测用户告知会收集行为数据
- `events` 表与 `companion_logs` 分离，`companion_logs` 24h 自动清理
- `events` 表建议保留期内测阶段 30 天，后续按需归档

---

## 附：与现有 `companion_logs` 的分工

| | `companion_logs` | `events`（新增） |
|---|---|---|
| 用途 | AI 管线调试日志 | 产品行为分析 |
| 内容 | 含图片、完整输入输出 | 仅元数据 |
| 保留 | 24h 自动清理 | 长期追加 |
| 容量 | 高 | 低 |
| 访问 | companion-logs.html | events.html |
