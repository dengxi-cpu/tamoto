# 埋点工作台使用说明

## 第一次上线前

1. 打开 Supabase 项目的 SQL Editor。
2. 复制并执行 `supabase/events.sql` 的全部内容。
3. 正常部署项目。
4. 部署完成后访问 `/events.html`。

如果希望工作台需要密码才能查看，在 Vercel 环境变量中新增：

```text
EVENTS_DASHBOARD_KEY=你自己设置的密码
```

设置后重新部署。打开工作台时，在右上角输入这个密码即可。

## 工作台能看到什么

- 进入专注页的人数、尝试开始次数和成功开始次数
- 每位用户的匿名 ID；同一浏览器会长期保持同一个 ID
- 专注完成率、平均有效专注时间、最常放弃的时间段
- AI 每次专注平均说几次、语音是否经常被中途打断
- AI 说完后用户有没有聊天、说话或点击角色
- 摄像头使用率
- 经典页与 Beta 页完成率对比
- 最近每一次专注的结果

新版工作台分为五个页面：

- **经营总览**：完成率、开始漏斗、每日趋势、放弃阶段、策略版本对比
- **用户与回访**：独立用户、次日回访、多日活跃、单用户专注历史
- **AI 陪伴效果**：开口次数、内容类型、说话间隔，以及说完后 5/15/30 秒的互动和退出
- **稳定性**：视觉耗时、聊天与 TTS 耗时、摄像头拒绝、离线与错误分布
- **会话时间线**：还原一次专注中暂停、切后台、AI 说话、用户互动和结束的先后顺序

每项分析都会显示样本数。样本太少时只用于发现问题，不建议据此立即调整 AI 策略。

## 数据何时出现

用户打开新版页面后，数据会先保存在用户设备中，再自动批量上传。通常 30 秒内出现在工作台；断网时不会丢失，恢复网络后会继续上传。

刚开始用户少时，工作台主要用于发现明显问题。积累更多专注记录后，再根据完成率和 AI 打断情况调整说话频率。

## 隐私

埋点不会上传聊天原文、语音内容、摄像头画面或任务正文，只记录消息长度、是否使用功能、时间和结果。

匿名用户 ID 只用于区分用户，不包含姓名、手机号等个人信息。用户更换浏览器、设备或清除网站数据后会得到新的 ID；未来接入登录后，才可以跨设备识别为同一个用户。
