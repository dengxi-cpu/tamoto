# 活人感陪伴系统方案总结

## 一、核心原则

不要让 AI 在“识别状态后触发固定台词”，而要让 AI 持续理解：

> 我们刚刚一起经历了什么，这件事与之前有什么关系，我此刻应当如何反应。

产品追求的不只是“AI 准确发现用户在玩手机”，而是让用户产生这样的感受：

> 它刚才真的看见我玩手机了，而且还记得我刚才说只玩五分钟。

这种对共同经历的连续理解，才是“活人感”的来源。

## 二、推荐架构

采用轻量的四层流水线：

```text
Camera
  ↓
VLM（眼睛：观察事实与变化）
  ↓
Memory LLM（大脑/导演：管理记忆并理解经历与关系）
  ↓
Actor LLM（演员：依据导演意图自然表达）
  ↓
TTS（声音：完成语气、停顿和节奏表演）
```

MVP 阶段不建议引入复杂 Multi-Agent、LangGraph、向量数据库或剧情状态机。可以先用一条简单管线实现：

```text
companion-pipeline.js

VLM → memoryLLM() → actorLLM() → TTS
```

## 三、各层职责

### 1. VLM：描述事件，不解释动机

VLM 只负责输出可见事实和相对上一轮发生的变化，不判断用户是否偷懒、逃避或敷衍。

```json
{
  "observation": "用户低头看着手里的手机，右手正在滑动屏幕。",
  "changes": [
    "上一轮用户正在看书",
    "现在拿起了手机"
  ],
  "visibleFacts": {
    "phone": true,
    "gaze": "down",
    "hands": "holding_phone",
    "position": "center"
  },
  "confidence": 0.93
}
```

设计重点是从识别 `STATE` 转向描述 `EVENT`。与 `state = PHONE` 相比，“用户从写字变成拿起手机”能给后续模型提供更多上下文。

### 2. Memory：记住经历，不堆积日志

记忆分为三层：

| 层级 | 内容 | 作用 |
| --- | --- | --- |
| Working Memory | 最近 1～3 分钟的原始事件 | 知道刚刚发生了什么 |
| Story Memory | 当前会话中经 Memory LLM 总结的经历 | 知道这一轮共同经历了什么 |
| Relationship Memory | 跨会话、具有长期价值的用户习惯与关系信息 | 体现“我认识你” |

示例：

```text
Working Memory
20:03:12 用户正在写字
20:03:17 用户停笔
20:03:22 用户拿起手机
20:03:27 AI：“嗯？”
20:03:31 用户：“我就看一下。”
20:03:37 用户仍在看手机
```

Story Memory 可概括为：“用户前 7 分钟较认真，第一次拿手机后承诺只看一下，短暂放下后又再次拿起。”

Relationship Memory 只保留长期有用的信息，例如：用户常以“五分钟”为借口、进入状态后通常能坚持较久、不喜欢专注时被频繁打扰、偏好具体而非泛泛的表扬。

会话结束后再进行慢速记忆沉淀：

```text
Working Memory
  → Memory LLM
  → Story Memory
  → Session 结束
  → Memory Consolidation
  → Relationship Memory
```

### 3. Memory LLM：管理记忆、理解剧情并决定是否开口

Memory LLM 同时承担记忆管理器和“导演”的职责。其输入包括：

- 旧的 Story Memory；
- 最近的 Observation Events；
- 最近的用户与 AI 对话；
- 当前任务和专注时长；
- 最新 VLM Observation；
- Relationship Memory。

它需要回答四个问题：

1. 刚刚发生了什么？
2. 当前事件与之前的经历有什么关系？
3. 角色此刻应处于什么情绪和关系状态？
4. 此刻应该说话，还是保持沉默？

建议输出：

```json
{
  "storyMemory": "用户第一次拿起手机后说只看一下，短暂放下后又再次拿起。",
  "currentSituation": "用户刚作出承诺，但现在再次拿起手机。",
  "behaviorChange": "再次分心，并与刚才的承诺产生矛盾。",
  "characterState": {
    "emotion": "amused_annoyed",
    "tension": "medium",
    "attitude": "有点不满，但主要是被用户气笑"
  },
  "responseIntent": "接住‘只看一下’进行调侃，不重复解释任务，不像老师。",
  "shouldSpeak": true
}
```

不使用 `warning_stage_2` 一类剧情状态。诸如“用户是否在敷衍”“角色是否该生气”“先沉默还是调侃”等关系和剧情判断，全部交给 Memory LLM。

### 4. Actor LLM：不分析，只演角色

Actor 不再判断事实、提醒时机或严厉程度，而只接收已经整理好的角色身份、关系状态、最近事件、当前感受和导演意图，并生成自然台词。

表达应接近真实视频通话：允许停顿、反问、半句话、重复和轻微语气词，不要求每次都输出完整、工整的句子。

同一事件应因角色设定产生不同反应，例如：

- 温柔年上：“怎么又拿起来了？刚刚不是才答应我吗。”
- Daddy：“宝宝。我刚才说什么了？”
- 毒舌青梅：“哇。你的五分钟还能续杯啊？”

### 5. TTS：表演，而不只是念字

Actor 除了台词，还应给出轻量表演参数：

```json
{
  "text": "……这就是你说的看一下？",
  "performance": {
    "emotion": "amused_annoyed",
    "intensity": 0.35,
    "pace": "slow",
    "pauseBefore": 600
  }
}
```

Speech 层据此控制停顿、语速、情绪、音量和 TTS style。TTS API 支持的参数直接传递，不支持的可以忽略。尤其应重视停顿，因为它对人格感和真实感影响很大。

## 四、沉默也是核心能力

`shouldSpeak` 必须是一等输出，不能默认每次观察后都说话。

用户持续认真写字时，系统可以不断更新记忆，但始终保持：

```json
{
  "shouldSpeak": false
}
```

AI 不频繁输出“继续加油”，反而更像一个真正陪伴中的人。只有以下情况才可能开口：

- 出现明显行为变化；
- 用户主动说话；
- 上一句话没有得到执行；
- 出现值得回应的成就；
- 达到长时间专注后的自然节点；
- 用户离开或回来。

前端可呈现 `SILENT`、`LISTENING`、`WATCHING`、`SPEAKING`，但这些只是表现状态，不是剧情状态机。

例如，用户重新拿起手机时可以先输出：

```json
{
  "shouldSpeak": false,
  "silentReaction": "watching"
}
```

角色只抬眼观察。若 10 秒后用户仍未放下，再决定开口。这种“先看着、再说话”的节奏会明显增强对面真实有人的感觉。

## 五、代码与 LLM 的职责边界

代码负责确定性机制：

- 时间和任务调度；
- VLM 调用间隔；
- 请求并发与互斥；
- TTS 播放状态及打断；
- 摄像头权限和网络异常；
- Memory 读写与持久化；
- 安全规则与失败兜底；
- `minimumSpeechInterval` 等硬限制。

LLM 负责语义和关系判断：

- 用户的行为与此前承诺是否矛盾；
- 当前角色应该有什么感受；
- 调侃、提醒、鼓励还是沉默更自然；
- 如何结合共同经历作出有角色感的反应。

原则是：代码控制系统，Memory LLM 记住经历并理解人。

## 六、MVP 落地建议

第一版只需要：

```text
companion-pipeline.js
session-memory.json
relationship-memory.md
```

建议实现顺序：

1. 将 VLM 输出从状态标签改为 Observation Event。
2. 建立最近 1～3 分钟的 Working Memory。
3. 实现 Memory LLM，输出更新后的记忆、情境理解、角色状态、导演意图和 `shouldSpeak`。
4. 将 Actor LLM 收敛为纯角色表演层。
5. 为 Actor 输出增加轻量 `performance` 参数并接入 TTS。
6. 加入调度、并发、异常兜底和 `minimumSpeechInterval`。
7. 会话结束时，将真正有长期价值的信息沉淀到 Relationship Memory。

在真实用户达到一定规模后，再考虑向量检索、记忆重要性评分、自动遗忘、长期关系图谱和多模态记忆。

## 七、方案总结

整套设计可以归纳为五句话：

> VLM 看见事实，不揣测。  
> Memory 记住经历，不堆日志。  
> Memory LLM 管理记忆、理解关系和剧情，不说台词。  
> Actor LLM 不分析，只做人。  
> TTS 不只是念字，而是在表演。

最终衡量标准不是识别准确率本身，而是用户是否感到：AI 看见了当下、记得住刚才、理解两者的关系，并以符合角色和关系的方式自然回应——包括恰当地保持沉默。
