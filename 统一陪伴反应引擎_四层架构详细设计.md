# 统一陪伴反应引擎：四层架构详细设计

> 范围：感知层 → 门控层 → 生成层 → 表达层
> 目标：把视觉监督、AI语音、氛围陪伴、聊天应答，统一成一套架构里的不同触发路径，而不是四套独立系统。

---

## 0. 设计原则

1. **感知与决策分离**：感知层只负责"发生了什么"，不做任何"要不要说话"的判断。判断逻辑全部下沉到门控层和生成层，避免感知模块里散落业务规则。
2. **门控层零AI调用**：门控层是纯规则/确定性逻辑，跑在每一次感知更新上，成本必须接近零，因为它是执行频率最高的一层。
3. **生成层一次调用，两种输出**：不拆成"先判断要不要说"再"生成内容"两次调用，合并成一次，让模型输出里带一个"是否说话"的字段。
4. **表达层与触发源解耦**：表达层不知道、也不需要知道这次输出是视觉触发的还是用户主动触发的，它只认"人设+内容+语气"。

---

## 一、感知层（Perception Layer）

### 1.1 职责

持续采集所有原始信号，归一化成统一的数据结构，写入一个共享的**会话上下文对象**（Session Context）。感知层不做任何"是否该说话"的判断，纯粹是数据采集与整理。

### 1.2 数据源与归一化

| 原始信号 | 来源 | 归一化后写入的字段 |
|---|---|---|
| 摄像头画面 | 每20秒采样一次，经视觉模型分类 | `last_visual_states: [{ts, label, confidence}]`（滚动窗口，保留最近N条）|
| 用户按住说话 | ASR识别文本 | `pending_user_input: {text, ts, source: "voice"}` |
| 用户打字 | 聊天输入框 | `pending_user_input: {text, ts, source: "text"}` |
| 沉默时长 | 本地计时器，基于最近一次"有意义事件"的时间戳计算 | `silence_seconds: number`（每次tick实时计算，不需要单独存储事件）|
| 任务与进度 | 专注页状态 | `task, elapsed_seconds, total_seconds, milestone_reached` |
| 人设与风格 | 角色配置 | `persona, encourage_style, push_style, push_intensity` |
| 最近对话历史 | 聊天记录/会话摘要 | `conversation_summary`（见4.3，滚动摘要，不是原始全量历史）|

### 1.3 统一会话上下文结构（Session Context）

建议用一个单一对象贯穿整场专注会话，所有层都读写这同一个对象，而不是各自维护状态：

```json
{
  "session_id": "xxx",
  "persona": { "name": "小艾", "style": "傲娇温柔型", "push_intensity": "略恼" },
  "task": { "name": "复习英语单词", "status": "学习" },
  "timer": { "elapsed": 720, "total": 1500, "milestone_reached": ["25%"] },
  "silence_seconds": 47,
  "last_visual_states": [
    {"ts": 1000, "label": "STUDYING", "confidence": 0.91},
    {"ts": 1020, "label": "PHONE", "confidence": 0.86},
    {"ts": 1040, "label": "PHONE", "confidence": 0.88}
  ],
  "pending_user_input": null,
  "last_spoken_at": 1560,
  "last_spoken_reason": "milestone_25pct",
  "conversation_summary": "用户今天状态不错，前半小时没有分心"
}
```

这个对象是**唯一事实来源**。门控层和生成层都从这里读数据，不允许绕开它单独查视觉模块或聊天模块的状态——这是避免系统重新"分裂"的关键工程约束。

### 1.4 更新机制

- **事件驱动优先**：用户说话、视觉分类结果变化、里程碑达成，都是"写入即触发"，立刻推一次门控检查。
- **兜底轮询**：即使没有任何事件，也建议一个低频（比如每5-10秒）的心跳，用于让`silence_seconds`持续增长后能触发"该说点什么表示还在"的判断，否则纯事件驱动可能永远等不到"仅仅是安静太久了"这种场景。

---

## 二、门控层（Gate Layer）

### 2.1 职责

在**每一次**感知层更新之后运行，用纯规则判断"这次值不值得让生成层看一眼"。门控层本身**不生成任何内容**，只输出一个布尔判断+触发理由。它的存在意义是把绝大多数"显然不该说话"的时刻挡在AI调用之外。

### 2.2 门控规则设计

建议按优先级从高到低设计规则链，命中即返回，不需要跑完所有规则：

```
规则1【用户主动输入】：pending_user_input 不为空
  → 直接放行，优先级=最高，可以打破冷却限制
  （用户主动说话永远值得回应，这是唯一不受冷却约束的触发源）

规则2【冷却检查】：距 last_spoken_at 不足冷却阈值（如3分钟）
  → 直接拦截，不放行（除非命中规则1）

规则3【视觉状态变化】：last_visual_states 中出现有意义的状态跳变
  （比如连续3次以上都是同一非STUDYING状态，且和上次判断的状态不同）
  → 放行，触发理由="视觉状态变化"

规则4【摸鱼持续阈值】：连续PHONE状态持续时间 ≥ 设定阈值，且不在冷却期
  → 放行，触发理由="持续分心"

规则5【里程碑达成】：milestone_reached 出现新元素（如刚跨过50%）
  → 放行，触发理由="进度里程碑"

规则6【长时间沉默】：silence_seconds ≥ 设定的"该表示还在"阈值（如8-10分钟），
  且当前不处于任何负面状态（不是PHONE/ABSENT）
  → 放行，触发理由="长时间安静，表达陪伴感"

规则7【置信度过滤】：任何视觉相关判断，如果 confidence 低于阈值
  → 直接拦截（哪怕命中规则3/4，也因为置信度不够而不放行，避免误判）

其余情况 → 拦截，本次tick结束，什么都不做
```

### 2.3 门控层输出结构

```json
{
  "should_consider": true,
  "trigger_reason": "持续分心",
  "priority": "normal",
  "can_interrupt_cooldown": false
}
```

`priority` 用于后续如果同一时刻有多个规则同时命中时的排序（比如用户主动说话的同时视觉也检测到摸鱼，应该优先处理用户主动输入）。

### 2.4 门控层的工程特性

- 纯函数、无副作用、无网络调用，理论上可以跑在客户端也可以跑在服务端，延迟应该在毫秒级。
- 可以用简单的状态机或规则引擎实现，不需要任何机器学习模型。
- 这一层的规则参数（冷却时长、阈值秒数）正是之前设计的"可反馈可调优系统"里，用户可调滑杆和运营侧调优真正落地修改的地方——门控层规则参数化，是那套调优机制的技术前提。

---

## 三、生成层（Generation Layer）

### 3.1 职责

只有门控层放行后才会被调用。生成层拿到完整会话上下文，调用大模型，一次性完成"要不要说话"和"说什么"两件事。

### 3.2 输入组装（Prompt Context Assembly）

给生成层的输入不是原始感知数据的堆砌，而是**筛选+摘要后的上下文**，建议包含：

- 人设描述、鼓励/督促风格、称呼
- 当前任务、进度百分比、已连续专注时长
- 本次触发原因（来自门控层的 `trigger_reason`）
- 最近视觉观察的**简要描述**，不是原始帧序列（比如"最近3分钟内玩手机2次，其余时间专注"）
- 会话摘要（不是完整聊天记录，见3.4）
- 如果是用户主动输入，附上用户说的原话

### 3.3 输出结构

```json
{
  "should_speak": true,
  "content": "……（角色台词）",
  "tone": "略带无奈但关心",
  "voice_style_hint": "傲娇"
}
```

`should_speak: false` 是完全合法的输出——即便门控层放行，生成层依然可以判断"这次还是不说更好"，这一步保留了最终的、需要理解上下文细腻程度的判断权。比如门控层因为"持续分心60秒"放行，但生成层结合会话摘要发现"用户刚刚说过在查资料"，可以判断这次不该提醒。

### 3.4 会话摘要的维护（避免上下文膨胀）

不要把完整对话历史每次都传给生成层。建议维护一个**滚动摘要**：

- 每次生成层调用完成后，用一个轻量摘要步骤（可以复用同一次调用，让模型顺带输出一句摘要更新，不需要额外调用）更新 `conversation_summary`。
- 摘要只保留"和陪伴质量相关"的信息（用户情绪、专注表现趋势），不需要逐字记录每句台词。
- 这样专注时长再长，喂给生成层的上下文体积也基本恒定。

### 3.5 失败降级

- 生成层调用超时或失败：直接判定为 `should_speak: false`，静默过去，绝不能让专注流程等待或报错。
- 连续失败：可以本地兜底用预设语录库（PRD里已有的"预设语录库"）顶替，保证即使AI服务异常，也能有基础的（非个性化）反馈，而不是完全沉默太久。

---

## 四、表达层（Expression Layer）

### 4.1 职责

统一负责把生成层的输出转成用户能感知到的形式——字幕+语音，不关心这次内容是怎么触发的。

### 4.2 关键设计点

- **播放队列**：如果短时间内有多个 `should_speak: true` 的结果排队（理论上门控层的冷却机制应该避免这种情况，但要有兜底），表达层需要一个简单队列，避免语音重叠播放。
- **打断优先级**：如果正在播放上一条语音时，用户突然主动说话（走的是门控规则1，最高优先级），表达层应该允许打断当前播放，优先响应用户——这是"随时可以跟TA说话"这个体验诉求在表达层的具体落地。
- **降级链路**：TTS失败时降级为纯字幕；字幕都渲染失败时，至少保证不阻塞专注计时本身。
- **风格一致性**：音色、语速、字幕的视觉样式都应该由 `persona` 决定，不因触发源不同而变化。

---

## 五、跨层的状态与并发控制

- **单一写入点**：Session Context 建议只允许感知层写入，门控层和生成层只读+写自己产出的判断结果字段（如 `last_spoken_at`），避免多处并发修改同一状态导致竞态。
- **并发触发的排队**：视觉tick和用户主动输入可能在极短时间内先后到达，门控层应该按到达顺序处理，但用规则1的高优先级保证用户输入即使晚到也能被优先处理（必要时可以让用户输入抢占正在进行中的生成层调用）。
- **延迟预算**：明确给每一层设定延迟预期，比如门控层<10ms，生成层<2-3秒（含TTS首包），超出预算的处理路径要有降级（比如生成层超时直接不说话，而不是让用户等）。

---

## 六、和"可反馈可调优系统"的对接

这套四层架构正好是之前设计的反馈机制的技术落点：

- **门控层的规则参数**（冷却时长、阈值秒数、置信度门槛）→ 对应用户可调的"视觉监督灵敏度"滑杆，以及运营侧基于聚合数据的调优。
- **生成层每次输出的 `trigger_reason` + 用户反馈（👍👎）** → 组成之前设计的决策日志，用于分析"哪类触发原因容易引起差评"，反过来调整门控层规则或生成层的Prompt措辞。
- 换句话说，反馈系统不是一个独立模块，它读写的正是这四层架构里已经存在的数据（门控输出、生成输出、用户反馈），不需要另外单独设计一套数据链路。

---

## 七、实施建议顺序

1. **先定义并落地 Session Context 的数据结构**，把现在分散在各模块里的状态收敛进来——这是一切的地基，务必先做。
2. **实现门控层**（纯规则，工作量小，见效快），先用现在PRD里已有的阈值参数（60秒/3分钟冷却等）作为初始规则，跑起来验证感知数据能否正确流入。
3. **改造生成层的调用方式**：把现有分散在"专注语生成""聊天回复""视觉提醒"里的AI调用逻辑，合并成同一个生成层入口，统一输入输出结构。
4. **表达层做打断与队列**，保证"随时说话"体验顺畅。
5. **最后接入反馈埋点**，跑通完整闭环。

这个顺序的好处是：即使中途暂停，每一步完成后系统都是可用的（不会出现"改了一半，专注页反而不能用了"的情况），是可以逐步替换现有实现、而不是推倒重来的路径。
